# R6 replacement cumulative reuse and admission review

Date: 2026-09-09. Status: reuse review complete; execution admission pending.

The repaired candidate can reuse the unchanged comparators' native products
and 8,000 already completed, truth-relative evaluation records. It cannot reuse
the previous candidate's measurements, Dask comparisons or scientific verdict.
This review starts no finder or evaluator and grants no execution authority.

## Identities and reuse decision

The replacement is implementation `4d0783728aaad3db1928972a73a28e71ace0f438`,
bound by `phase-5-r6-estimator-repair-identity-review.json`, SHA-256
`f7afe3c18cf2c1c98397e688b58d62b88052c15d6c0705d1930e24c22e43f78f`.
Its source SHA-256 is
`cc1db52e4e3088794b8d5c4f41b9301daedb848bcd5a282a3b6b381b585a7715`;
configuration remains
`5eca0efc1995f206900506c87f2ca3cba6aa4a2f1a5155bc3e58863bf3cb75c4`.
Public composition is the reviewed observable-source/joint-estimator v10.

Preserve failed R6 terminal `7146f2e8...`, capture seal `e1bc9a91...` and
combined evaluation seal `d14205d9...` in their original namespaces. The
prospective machine-readable review contains their full digests. No old pass,
failure or incumbent-uncertainty acceptance is rewritten or transferred.

| Evidence | Retain/reuse | New work for this candidate |
| --- | --- | --- |
| Frozen inputs and retained external products | All 2,400 inputs and 9,600 reference runs | None |
| Incumbent Hebog | 2,400 captures and 2,400 evaluation records | No incumbent execution or rematching |
| Released PyBDSF | 2,400 evaluation records | No PyBDSF execution or rematching |
| Pinned PyBDSF master | 2,400 evaluation records | No PyBDSF execution or rematching |
| Aegean | 800 compact evaluation records | No Aegean execution or rematching |
| Previous current-Hebog candidate | Preserve as historical failed evidence only | 2,400 new Serial captures and evaluations |
| Existing-Dask conformance | Preserve old 12 as historical only | 12 comparisons against new Serial captures |
| Paired statistics and terminal | Preserve old terminal only | Recompute the unchanged comparisons for the new candidate |

The 8,000 reusable records occupy 766,667,871 logical bytes. Their sorted
binding index (input ID, finder ID, original path, file SHA-256) has canonical
SHA-256 `c3fdb6c3ae0e51bbaae1b4f3f46cfe32b5493b2d054ffca080fee26e4db80d25`.
Reuse means reading those exact bytes, not serializing equivalent replacements.
No duplicated reference array store is needed. The complete new decision still
requires 10,400 records: 8,000 retained plus 2,400 new candidate records.

## Why reuse is scientifically valid

Each retained record measures its finder independently against analytic
injected truth. Neither PyBDSF nor the incumbent supplies truth for Hebog.
Pairing those unchanged observations with a changed candidate is a new
comparison, not a new score for the old finder.

Compared with continuation implementation `b64228db...`, the per-input
evaluator, native readers/source-union adapter, truth compiler, source matcher,
metric rules and statistical compiler are unchanged. Five central validation
modules are byte-identical and bound in the review record. The only changed
definitions in `validation/products.py` construct Hebog source measurements
and attribution. Its native catalogue/FITS readers and their private Aegean
helpers are unchanged. The changed fitting/source-domain routines do not run
when loading the retained incumbent association or PyBDSF native catalogues.
Additional measurement-disposition fields describe new Hebog captures;
reference records have no such generated dispositions.

Preserve the approved unavailable-source-support amendment and both historical
diagnostic versions already admitted by the closed continuation. A native
source without exclusive pixels remains a catalogue row with unavailable
support; do not manufacture pixels, drop it or replace its measurement with an
island. Reuse consumes the sealed observations directly; it does not introduce
a new public legacy reader or migrate old catalogue files.

Before execution, verify every reusable binding and its parent terminal/seal,
input/recipe, finder identity, native capture, truth and metric-program context.
Reject duplicate or missing keys, stale programs, altered artifacts or any
attempt to include old `current-hebog` observations. Bind the complete
transitive evaluator/contract identity at the execution freeze, not just the
five central modules. A later metric, truth or adapter change invalidates this
reuse decision and requires review; an old record hash alone is insufficient.

The existing read-only post-terminal verifier (`4fd0c9f5...`) was rerun from
the immutable continuation checkout, with its original source/environment.
It passed at 07:34 UTC on 2026-09-09 after approximately eight minutes:
2,400 input bundles, 9,600 reference runs, 4,800 native captures, 12 historical
Dask comparisons and all 10,400 completed records verified. It performed zero
finder executions and zero evaluations; the failed terminal and old partial
directories remain unchanged. This is evidence-integrity verification, not
an execution preflight for the new candidate.

## Population and decision rules

Keep all 800 compact inputs and all four 400-input Continuum datasets. This
preserves the existing SNR, blend, clipped/corner, invalid-pixel, curved/shell,
extended-neighbour and background-risk populations; no difficult cell is
removed to meet the budget. Use the existing deterministic 12-input Dask
selection, two workers and one numerical thread per worker.

The population (`0bd3e6a6...`) and prospective registry (`095354bc...`) remain
frozen. Retain all 1,187 binding comparisons and five safety checks, both
PyBDSF comparators, Aegean compact parity and incumbent retention. Preserve
the original confidence algorithms, 50,000 bootstrap resamples, seed 20260810,
practical margins and thresholds. Sample size is the unchanged prospective
regression design, not a newly claimed power guarantee. Underpowered remains
underpowered; the prior human uncertainty acceptance does not transfer.

Interpret compact science first, then Continuum, before runtime. Require
`cumulative_science_regression_ready=true`, `all_required_endpoints_pass=true`
and every binding comparison/safety check to pass. A scientific failure is
terminal. Any later fresh sentinel needs unopened, disjoint seeds and separate
admission after cumulative success; it is a falsification check, not powered
parity or permission to close Phase 5 automatically.

## Disk and time admission

Measured logical storage from the preserved full capture set is 30,160,058,240
bytes (28.09 GiB) for old current-Hebog and 8,509,292,168 bytes (7.92 GiB) for
the incumbent. Old current-Hebog evaluation records occupy 268,301,720 bytes.
Reusing the incumbent and external records avoids those new copies, but the
old evidence must remain present alongside the replacement.

A provisional **68 GiB free-space budget** allows 1.5 times the old candidate
capture volume (42.13 GiB), 2 GiB for new diagnostic records, 3 GiB for working
space and 20 GiB untouched headroom, rounded up. This is a planning allowance,
not a proven upper bound for the repaired fits or denser telemetry. The
read-only snapshot found 63,433,973,760 free bytes (59.08 GiB): roughly **9 GiB
short**. Recheck actual space and a short independent-fixture size probe before
admission. Do not delete historical evidence, notebook products or Podman data
as part of this review. No new Podman execution or container growth is planned.

Historical stage timestamps give the following engineering observations, not
a controlled performance benchmark:

- The old two-worker current/incumbent capture completion span was about
  4 h 35 min, excluding startup/preflight and the first pair's work.
- Its first 800 compact evaluations took about 32 minutes. The continuation's
  1,592 remaining four-finder Continuum evaluations took about 40 minutes.
- The unchanged final statistics took about 3 h 9 min after the aggregation
  stage marker. Avoiding reference execution does not remove this cost.
- The previous exhaustive post-terminal hash audit took about seven minutes.

For planning only, allowing 4.6--9.2 hours for changed candidate captures,
up to 1.25 hours for candidate evaluation, approximately 3.2 hours for existing
statistics and 0.5 hours for checks/Dask gives roughly **9--14 hours**.
The capture allowance is deliberately uncertain: the new joint estimators
have not been timed on an admitted representative cost ladder. It is not a
guarantee and must be replaced by a measured estimate before execution.

The user's sub-12-hour requirement was for the final campaign; the original
R6 regression admission explicitly distinguished that from its 10--24-hour
estimate. Even so, the replacement must not silently promise or assume a
12-hour ceiling. First measure the exact repaired fixture capture/evaluation
path and account for statistics. If the resulting conservative estimate still
exceeds the applicable budget, surface that conflict before launching. Do not
reduce sample count, known-risk geometries, comparators or bootstrap rules.

## Remaining implementation and launch gates

The old runner is **not** safe to reuse unchanged: `capture_pair` executes
both candidates, and `evaluate_captured_input` evaluates every finder. The old
continuation only accepts its closed original candidate and output. Neither
entry point should be repointed or have its consumed decision edited.

1. Test-first, add a narrow reuse-aware orchestration path around the existing
   capture, per-finder diagnostic and aggregate functions. Keep the ordinary
   full-census validator intact; make retained/new roles explicit. Do not
   create a second scientific evaluator or change historical bound programs.
2. On synthetic temporary artifacts, exercise new current capture through
   readback/evaluation, v10 attribution, reuse of unchanged reference records,
   spawn/existing-Dask, aggregate handoff and atomic terminal publication.
   Inject missing/duplicate/foreign/stale records, changed hashes, wrong
   candidate, output collision, insufficient disk and process failure. Prove
   no incumbent/reference execution and no rewriting of reused records.
3. Run an independent, bounded geometry/cost ladder including compact blends,
   clipped corners, curved/shell and dense extended guards. Record full
   capture/evaluation times, bytes, environment and limitations before revising
   disk/time admission. Do not probe viewed campaign images or inspect new
   partial scientific results to choose the population.
4. Pass focused tests, coverage/changed-branch checks, Serial/Dask, equivalence,
   docs, review and clean final hooks. Freeze the exact committed programs,
   candidate, reusable-record inventory, runtime, population, gates and a new
   write-once output/scratch identity. Repeat the exhaustive no-write preflight.
5. Only then resolve exact execution authority and launch once. Monitor hourly
   for process health, counts, disk and atomic terminal, not partial science.
   Stop on a scientific failure; process repair does not permit tuning.

The next task is this fixture-only reuse-aware runner and cost validation.
No executable identity, run directory, decision, campaign or automatic monitor
was created by this admission review.
